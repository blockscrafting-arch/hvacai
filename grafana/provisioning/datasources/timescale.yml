apiVersion: 1

# Старый datasource без uid ломает панели с ссылкой uid: tsdb-hvac — удаляем и создаём заново при старте Grafana
deleteDatasources:
  - name: TimescaleDB
    orgId: 1

datasources:
  - name: TimescaleDB
    uid: tsdb-hvac
    type: postgres
    url: timescaledb:5432
    # Явные значения: Grafana не всегда подставляет ${VAR} из env в provisioning (пустой database → ошибка в панелях)
    database: hvac
    user: postgres
    secureJsonData:
      # Пароль из env контейнера grafana (см. docker-compose)
      password: $POSTGRES_PASSWORD
    jsonData:
      sslmode: disable
      postgresVersion: 1600
      timescaledb: true
    isDefault: true
    editable: true
